import 'package:flutter/material.dart';

class SubpageInfo extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Column(
      children: <Widget>[Text("Info Page")],
    );
  }
}
